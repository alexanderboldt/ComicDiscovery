<resources>

    <!-- Theme for Splash-screen -->
    <style name="SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="textHeadline5OnBackground">
        <item name="fontFamily">@font/source_sans_pro_regular</item>
        <item name="android:textSize">@dimen/textAppearanceHeadline5</item>
        <item name="android:textColor">@color/colorOnBackground</item>
    </style>

    <style name="textHeadline6OnBackground">
        <item name="fontFamily">@font/source_sans_pro_semi_bold</item>
        <item name="android:textSize">@dimen/textAppearanceHeadline6</item>
        <item name="android:textColor">@color/colorOnBackground</item>
    </style>

    <style name="textBody1OnBackground">
        <item name="fontFamily">@font/source_sans_pro_regular</item>
        <item name="android:textSize">@dimen/textAppearanceBody1</item>
        <item name="android:textColor">@color/colorOnBackground</item>
    </style>

</resources>